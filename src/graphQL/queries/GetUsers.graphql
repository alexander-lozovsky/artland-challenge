query GetUsers($query: String!, $first: Int!) {
  search(type: USER, query: $query, first: $first) {
    nodes {
      ... on User {
        name
        login
        starredRepositories {
          totalCount
        }
        repositories {
          totalCount
        }
      }
    }
  }
}
